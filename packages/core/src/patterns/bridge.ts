/**
 * Decouple an abstraction from it's implementation so that the two can vary independently.
 * This interface act as a bridge to different implementations of same method name.
 */
export interface BridgeInterface{

}