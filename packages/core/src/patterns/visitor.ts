/**
 * Allows for one or more operation to be applied to a set of objects at runtime, decoupling the operations from the object structure.
 * Implement visitor methods.
 * @see VisitorContext | to call visitor methods independent from their implementation.
 */
export interface Visitor{
    
}

/**
 * Allows for one or more operation to be applied to a set of objects at runtime, decoupling the operations from the object structure.
 * Visitor method caller independent from it's implementation.
 * @see Visitor | to implement visitor methods.
 */
export interface VisitorContext{

}