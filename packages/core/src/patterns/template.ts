/**
 * Define the basic steps of an algorithm and allow the implementation of the individual steps to be changed.
 * Configure parameters of Abstract template
 * @see AbstractTemplate | to define default parameters and algorithm based on parameters.
 */
export interface Template{

}

/**
 * Define the basic steps of an algorithm and allow the implementation of the individual steps to be changed.
 * Define default parameters and algorithm based on parameters.
 * @see AbstractTemplate | to implement different algorithm variants with different parameter values.
 */
export interface AbstractTemplate{

}