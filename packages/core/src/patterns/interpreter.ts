/**
 * Given a language define a representation for it's grammar along with an interpreter that uses the representation to interpret sentences in the language.
 * Interprets context to a value.
 * @see InterpreterContext | to implement interpreter input data.
 */
export interface Interpreter{
    
}

/**
 * Given a language define a representation for it's grammar along with an interpreter that uses the representation to interpret sentences in the language.
 * Interpreter methods input context.
 * @see Interpreter | to interpret context to desigred values.
 */
export interface InterpreterContext{

}