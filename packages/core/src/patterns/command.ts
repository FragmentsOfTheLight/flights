/**
 * Create objects which encapsulates actions and parameters.
 * Encapsulates parameters of function call to be called later.
 */
export interface Command{
    execute(): any
}

/**
 * Create objects which encapsulates actions and parameters.
 */
export interface CommandInvoker{

}